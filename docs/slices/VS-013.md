# VS-013 — Motores: fórmula + reglas condicionales

Milestone: M10. Cierra los dos tipos de Elemento pendientes desde M4 (VS-007): `calculado` (`engine/formula`) y la visibilidad condicional `visibleIf` (`engine/rule`).

## Especificación previa

`../engines/formula.md` y `../engines/rule.md` (diseño completo: parser/evaluador de expresiones a mano, por qué `visibleIf` es una propiedad y no un Elemento contenedor, validación cruzada de ciclos/autorreferencia, integración con progreso/exportación). No se repite aquí.

## Contratos

- `packages/sdk-core/src/formula.ts` (nuevo): `parseFormula`, `extractExpressionReferences`, `evaluateExpression`, `FormulaSyntaxError`.
- `packages/sdk-core/src/rule.ts` (nuevo): `condition` (zod), `Condition`, `isElementVisible`.
- `packages/sdk-core/src/response.ts`: `hasAnswer` (nuevo, criterio compartido de "¿tiene respuesta?").
- `packages/sdk-core/src/form-schema.ts`: rama `calculado` en `formElement`; `visibleIf?: Condition` en `formElementBase`; `.superRefine()` en `formSchema` (ciclos de fórmulas + autorreferencia en `visibleIf`).
- `packages/sdk-core/src/component-registry.ts`: `{ type: "calculado", label: "Calculado", isQuestion: true, version: 1 }`.
- UI Builder (`.../subindicators/[subindicatorId]/page.tsx`): config de `expression`/`decimals` para `calculado`, editor de `visibleIf` para cualquier Elemento.
- UI Runtime (`apps/web/app/evaluations/[token]/page.tsx`): filtra Elementos por `isElementVisible` (progreso + render), recalcula y autoguarda `calculado` en vivo.
- API export (`apps/web/app/api/evaluations/[id]/export/route.ts`): filtra filas por `isElementVisible`.

## Fuera de alcance (explícito)

Ver `../engines/formula.md`/`../engines/rule.md` (funciones tipo SUM/AVG, referencias entre Subindicadores, combinación AND/OR de condiciones, ocultar Dimensión/Indicador/Subindicador completo, edición manual del valor calculado).

## Aceptación

- [x] `formula.ts`/`rule.ts`/`hasAnswer` con tests en `packages/sdk-core` (48+26 tests).
- [x] `formElement`/`formSchema` (con `.superRefine()` de ciclos/autorreferencia) con tests en `packages/sdk-core` (27 tests) — 144 tests en total en `packages/sdk-core`.
- [x] `pnpm slice:close` en verde (5 tasks build, 144 tests, 5 tasks typecheck).
- [x] Flujo verificado en navegador real **contra producción**: Subindicador con `seleccion_unica` + `visibleIf`, dos `numero` y un `calculado` (`{n1} + {n2}`) — el elemento condicionado aparece/desaparece en vivo al cambiar la respuesta de la que depende, el progreso sube de 0%→20%→40%→80%→100% correctamente excluyendo el elemento oculto del denominador, el calculado se autoguarda (persiste tras recargar), y el CSV exportado incluye "Total" (tipo Calculado, valor 15) y excluye "Detalle" mientras está oculto. Fórmula con ciclo (`{c2}`↔`{c1}`) rechazada por la API con 400 y mensaje `"Referencia circular en fórmulas: c1 → c2 → c1"`. Datos de prueba limpiados.
- [x] `docs/CHANGELOG.md`, `docs/checkpoints/CHECKPOINT.md`, `docs/BACKLOG.md`, `docs/project_notes/issues.md` actualizados.

## Estado

Completado 2026-08-05.
