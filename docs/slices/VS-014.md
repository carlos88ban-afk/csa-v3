# VS-014 — Permisos (RBAC: dueño / editor / evaluador)

Milestone: M11. Refina el rol binario `owner`/`member` (M1/VS-003) en tres roles con permisos distintos, y expone por primera vez en la app la gestión de miembros/invitaciones.

## Especificación previa

`../engines/permission.md` (diseño completo: por qué sin `access-control` custom de Better Auth, matriz de permisos, por qué invitar/listar/gestionar miembros no necesita rutas API nuevas). No se repite aquí.

## Contratos

- `packages/db/src/authz.ts`: `requireWriteAccess(headers)` (nuevo), `AuthzError` gana el código `"FORBIDDEN"`.
- Rutas de escritura del dominio (`POST`/`PATCH`/`DELETE` en frameworks/dimensions/indicators/subindicators/evaluations) cambian `requireActiveMember` → `requireWriteAccess`. Rutas de lectura (`GET`, incluida `evaluations/[id]/export`) no cambian.
- UI: `apps/web/app/organizations/page.tsx` (sección de miembros/invitaciones, solo `owner`), `apps/web/app/accept-invitation/[invitationId]/page.tsx` (nueva).

## Fuera de alcance (explícito)

Ver `../engines/permission.md` (access-control granular por recurso, múltiples `owner`, envío de invitación por email, permisos sobre Evidencias/Respuestas del evaluado).

## Aceptación

- [x] `requireWriteAccess` con test de integración en `packages/db` contra Neon real (crea miembros `editor`/`evaluador` de verdad vía `auth.api`, confirma que `evaluador` es rechazado y `owner`/`editor` permitidos, y que la lectura sigue abierta a los tres) — 22 tests en total en `packages/db`.
- [x] `pnpm slice:close` en verde (5 tasks build, 145 tests, 5 tasks typecheck).
- [x] Flujo verificado en navegador real **contra producción**: sección "Miembros" en `/organizations` lista al `owner` correctamente; invitar a un email nuevo como `evaluador` genera el link de aceptación (`authClient.organization.inviteMember` real contra prod); la página `/accept-invitation/[id]` carga y, al intentar aceptar con una sesión de email distinto, Better Auth rechaza correctamente ("You are not the recipient of the invitation") y la UI lo muestra. Invitación de prueba cancelada al terminar.
- [x] **Límite de la verificación manual, documentado explícitamente** (ver `docs/project_notes/decisions.md`, entrada 2026-08-05): no se pudo probar el flujo completo de aceptación + intento de escritura con una segunda cuenta real, porque crear una cuenta nueva requiere escribir una contraseña — acción prohibida sin excepciones por las reglas de seguridad de la sesión. La corrección de `requireWriteAccess` (rechazo real de `evaluador`, permiso a `owner`/`editor`) queda respaldada por el test de integración contra Neon real (mismo dato que usa producción) más revisión de código de las 10 rutas gateadas — no por un click-through de dos usuarios distintos en el navegador.
- [x] `docs/CHANGELOG.md`, `docs/checkpoints/CHECKPOINT.md`, `docs/BACKLOG.md`, `docs/project_notes/issues.md` actualizados.

## Estado

Completado 2026-08-05.
