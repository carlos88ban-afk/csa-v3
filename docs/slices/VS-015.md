# VS-015 — Accesibilidad (WCAG 2.2 AA)

Milestone: M12, último del roadmap original. Cierra NFR-5. Sin motor de dominio nuevo — transversal sobre las ~13 pantallas existentes vía tokens/componentes compartidos.

## Especificación previa

`../architecture/accessibility.md` (diseño completo: hallazgos reales de contraste/tamaño/navegación, correcciones, qué ya cumplía). No se repite aquí.

## Contratos

- `apps/web/app/globals.css`: tokens `--border`/`--good`/`--warn` ajustados (contraste), `.btn--sm` gana `min-width`/`min-height: 24px`, `.skip-link` nuevo.
- `apps/web/app/layout.tsx`: link "Saltar al contenido" + wrapper `id="main-content"` alrededor de `{children}`.
- `apps/web/app/frameworks/.../subindicators/[subindicatorId]/page.tsx` y `apps/web/app/evaluations/[token]/page.tsx`: contenedor de estado de autosave gana `aria-live="polite"`.

## Fuera de alcance (explícito)

Ver `../architecture/accessibility.md` (auditoría de los ~50 criterios AA, pruebas con lector de pantalla real, i18n/traducciones, axe-core/Playwright, polish visual no ligado a un hallazgo concreto).

## Aceptación

- [ ] `pnpm slice:close` en verde (build, tests, typecheck) — sin tests nuevos (cambio de CSS/markup, no de lógica de dominio).
- [ ] Contraste recalculado con la fórmula de luminancia relativa de WCAG confirma ≥3:1 (`--border`) y ≥4.5:1 (`--good`/`--warn` como texto de `Pill`) en ambos modos.
- [ ] Verificado en navegador real **contra producción**: árbol de accesibilidad de login/Form Editor/Runtime sin regresiones, "Saltar al contenido" aparece como primer elemento al presionar Tab y funciona, foco visible en toda la navegación por teclado, autosave anunciado vía `aria-live`.
- [ ] `docs/CHANGELOG.md`, `docs/checkpoints/CHECKPOINT.md`, `docs/BACKLOG.md`, `docs/project_notes/issues.md` actualizados.

## Estado

En progreso — iniciado 2026-08-05.
