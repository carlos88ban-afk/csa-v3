# VS-012 — Exportación de resultados (CSV)

Milestone: M9. Cierra F5 (`OBJECTIVES.md`): un miembro de la Organización puede descargar en CSV las Respuestas capturadas por una Evaluación publicada.

## Especificación previa

`../engines/export.md` (diseño completo: por qué CSV plano y no Excel/PDF, formato de filas/columnas, UTF-8 con BOM, quién puede exportar, contratos, rutas). No se repite aquí.

## Contratos

- `packages/db/src/domain/evaluation-service.ts`: `getEvaluation(organizationId, id)` (nuevo, lookup tenant-scoped, mismo patrón que `getFramework`).
- API: `GET /api/evaluations/[id]/export` (autenticado, tenant-scoped) — responde `text/csv`.
- UI: link "Exportar CSV" en la lista de Evaluaciones publicadas de `apps/web/app/frameworks/[frameworkId]/page.tsx`.

## Fuera de alcance (explícito)

Ver `../engines/export.md` sección "Fuera de alcance" (XLSX/PDF, envío por email, exportación agregada multi-Evaluación, analítica/BI/scoring, exportación desde la ruta pública del evaluado).

## Aceptación

- [x] `getEvaluation` tenant-scoped con test de integración en `packages/db` (devuelve `null` para una Evaluación de otra Organización) — 21 tests en total (`packages/db`).
- [x] `pnpm slice:close` en verde (5 tasks build, 61 tests, 5 tasks typecheck).
- [x] Flujo verificado en navegador real **contra producción**: publicar una Evaluación con preguntas de texto/selección única/selección múltiple, responder, exportar el CSV desde la página de Framework (click real + verificación por `fetch`), confirmar filas/columnas correctas, labels (no ids) en selección única/múltiple, comas dentro de una respuesta correctamente escapadas (RFC 4180), tildes/ñ legibles, BOM UTF-8 presente en los bytes crudos (`EF BB BF`), pregunta sin responder con celda vacía, `id` inexistente → 404, sin sesión → 401. Datos de prueba limpiados.
- [x] `docs/CHANGELOG.md`, `docs/checkpoints/CHECKPOINT.md`, `docs/BACKLOG.md`, `docs/project_notes/issues.md` actualizados.

## Estado

Completado 2026-08-05.
