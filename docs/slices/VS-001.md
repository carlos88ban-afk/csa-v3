# VS-001 — Scaffold monorepo

Milestone: M0

## Contratos

pnpm + Turborepo + TS strict + Vitest + Playwright (base) + CI (GitHub Actions, con cache).

## Aceptación

- [x] `pnpm build`, `pnpm test`, `pnpm typecheck` verdes localmente.
- [x] CI configurado (`.github/workflows/ci.yml`) — pendiente de verificar en push real al remoto.
- [x] Docs de arranque (`README.md` raíz).

## Estado

Completado 2026-08-04. `packages/sdk-core` sirve como paquete mínimo real (no vacío) para que el gate de CI sea significativo desde el inicio.
