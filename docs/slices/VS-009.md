# VS-009 — Publicación + enlaces seguros

Milestone: M6. Primer motor que produce algo visible fuera del Builder: un Framework se puede publicar como Evaluación y compartirse por enlace, sin necesidad de cuenta para verlo.

## Especificación previa

`../engines/publishing.md` (diseño completo: por qué un snapshot completo en vez de un puntero a `revisionNumber`, estructura, contratos, rutas). No se repite aquí.

## Contratos

- `packages/db/src/schema/evaluation.ts` (nuevo): tabla `evaluation` (id/organizationId/frameworkId/token/title/snapshot/publishedAt/createdAt).
- `packages/sdk-core/src/evaluation.ts` (nuevo): `createEvaluationInput`, `evaluationSnapshot` (zod), `Evaluation`.
- `packages/db/src/domain/service.ts` (o archivo nuevo dedicado): `createEvaluation`, `listEvaluations`, `deleteEvaluation`, `getEvaluationByToken` (sin `organizationId` — ver spec).
- API: `POST /api/evaluations`, `GET /api/evaluations?frameworkId=`, `DELETE /api/evaluations/[id]` (autenticados) + `GET /api/public/evaluations/[token]` (sin autenticación, bajo prefijo `public/` a propósito).
- UI: botón "Publicar" + lista de Evaluaciones en `apps/web/app/frameworks/[frameworkId]/page.tsx`; página pública nueva `apps/web/app/evaluations/[token]/page.tsx`.

## Fuera de alcance (explícito)

Ver `../engines/publishing.md` sección "Fuera de alcance" (captura de respuestas/M7, tabla de historial de revisiones, expiración automática del enlace, RBAC granular, congelamiento diferenciado por campo).

## Aceptación

- [ ] `evaluationSnapshot` (zod) con tests en `packages/sdk-core`.
- [ ] Test de integración en `packages/db`: publicar produce un snapshot fiel; editar el Framework/Subindicador después de publicar no afecta la Evaluación ya creada; `getEvaluationByToken` no requiere sesión.
- [ ] `pnpm slice:close` en verde.
- [ ] Flujo verificado en navegador real **contra producción**: publicar desde el Builder → copiar link → abrirlo en una pestaña sin sesión → ver el contenido → revocar → confirmar que el link deja de resolver.
- [ ] `docs/CHANGELOG.md`, `docs/checkpoints/CHECKPOINT.md`, `docs/BACKLOG.md`, `docs/project_notes/issues.md` actualizados.

## Estado

En progreso — iniciado 2026-08-05.
