# Contratos de API

Pendiente. Se documenta a partir de VS-004 (dominio core CRUD), SDK-first: los contratos viven primero en `packages/sdk-core` y se documentan aquí (endpoints REST versionados, errores, idempotencia) antes de exponerse.
